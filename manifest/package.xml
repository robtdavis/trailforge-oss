<?xml version="1.0" encoding="UTF-8"?>
<Package xmlns="http://soap.sforce.com/2006/04/metadata">

    <!-- Core TrailForge custom objects -->
    <types>
        <members>Course__c</members>
        <members>Module__c</members>
        <members>Lesson__c</members>
        <members>Enrollment__c</members>
        <members>Progress__c</members>
        <members>CertificateTemplate__c</members>

        <!-- Quiz data model -->
        <members>Quiz__c</members>
        <members>Quiz_Attempt__c</members>
        <members>Quiz_Question__c</members>
        <members>Quiz_Answer_Option__c</members>
        <members>Quiz_Response__c</members>

        <!-- Content security custom metadata type -->
        <members>Allowed_Content_Domain__mdt</members>

        <name>CustomObject</name>
    </types>

    <!-- Custom Metadata records (Allowed_Content_Domain__mdt rows, etc.) -->
    <types>
        <members>*</members>
        <name>CustomMetadata</name>
    </types>

    <!-- Apex classes (DAO, services, controllers, batches, etc.) -->
    <types>
        <members>*</members>
        <name>ApexClass</name>
    </types>

    <!-- Apex triggers -->
    <types>
        <members>*</members>
        <name>ApexTrigger</name>
    </types>

    <!-- LWC bundles (lessonPlayer, quizBuilder, etc.) -->
    <types>
        <members>*</members>
        <name>LightningComponentBundle</name>
    </types>

    <!-- Aura components (if we ever add any) -->
    <types>
        <members>*</members>
        <name>AuraDefinitionBundle</name>
    </types>

    <!-- Lightning pages (App / Record pages) -->
    <types>
        <members>*</members>
        <name>FlexiPage</name>
    </types>

    <!-- Tabs for TrailForge pages/components -->
    <types>
        <members>*</members>
        <name>CustomTab</name>
    </types>

    <!-- Permission sets for Learner/Admin -->
    <types>
        <members>*</members>
        <name>PermissionSet</name>
    </types>

    <!-- Static resources (CSS, JS, images, etc.) -->
    <types>
        <members>*</members>
        <name>StaticResource</name>
    </types>

    <!-- Flows (e.g., Progress, admin helper flows, etc.) -->
    <types>
        <members>*</members>
        <name>Flow</name>
    </types>

    <version>65.0</version>
</Package>